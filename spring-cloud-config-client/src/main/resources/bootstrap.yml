spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:19600/
  rabbitmq:
    host: localhost
    port: 5672
    username: niushuai233
    password: niushuai233
management:
  endpoints:
    web:
      exposure:
        include: "*"
logging:
  level:
    org.springframework.web: trace